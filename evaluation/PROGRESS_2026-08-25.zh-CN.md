# ICON 复现与评测进度：2026-08-25

## 今天确认完成

1. 官方预训练推理链已经跑通，官方样例得到可检查的最终 Mesh。
2. 自建评测层已经通过同一网格、平移网格和额外碎片三类行为测试。
3. 已完成 ICON-filter 在 CAPE 150 个测试样本、每人三个旋转下的 450 例
   可恢复评测；逐例保存预测 Mesh、真值 Mesh、可视化、指标、耗时、显存、
   代码/配置/权重哈希和网格拓扑信息。
4. 自建 450 例评测没有记录推理失败。当前描述性汇总为：

   - easy 组 150 例：Chamfer 0.79946 cm，P2S 0.76830 cm，法向误差
     0.05049；
   - hard 组 300 例：Chamfer 0.88423 cm，P2S 0.86346 cm，法向误差
     0.05280；
   - 去除每次启动的 CUDA 预热样本后，449 例平均重建时间约 1.58789 秒，
     平均峰值已分配显存约 3.54650 GiB。

   这些数字只描述当前 checkout、当前 150x3 样本和当前评测合同。由于它们
   与论文表格存在明显差异，不能直接写成“论文表格已经复现”。
5. 官方 `apps.train -test` CAPE 校准运行已经完成 450/450，并生成
   `results/icon-filter/cape/test_results.npy` 和逐例证据。下一步需要提取
   官方汇总并与自建评测逐项核对，定位差异来源。
6. 已建立下一阶段“人体先验来源配对实验”协议：同一张 CAPE 图像分别使用
   CAPE 准备的 SMPL 和 PIXIE 从图像估计的 SMPL-X，测量从标准化研究条件
   走向真实端到端使用条件的性能损失。

## 今天不能声称完成的内容

- 不能声称已经严格复现论文 CAPE 表格；官方入口与自建入口的差异尚未
  审计完毕。
- 不能把 `apps.infer` 的输出直接与 CAPE 真值计算距离；裁剪、相机尺度、
  平移、旋转和坐标轴必须先被映射回 CAPE 米制坐标系。
- 尚未得到 PIXIE 估计先验与 CAPE 准备先验的有效配对结果。
- 尚未运行 PIFu、PaMIR 和 ICON-no-filter 的公平横向比较。
- 尚未做需要重新训练 checkpoint 的正式特征消融。

## 明天按顺序继续

1. **官方校准审计**：读取官方 `test_results.npy`，保存人可读文本，并和
   自建 450 例总体/easy/hard 指标核对。若一致，确认评测外壳可信；若不
   一致，检查样本顺序、旋转、聚合公式、随机采样和 mesh 清理。
2. **坐标适配器设计与单元测试**：记录 `apps.infer` 的裁剪框、512 缩放、
   PIXIE 弱透视相机、平移和 CAPE 相机旋转；禁止静默 ICP 或逐例最佳缩放。
3. **2 例坐标闸门**：一易一难，只给 CAPE 渲染图，让 PIXIE 估计人体；
   显示原图、估计 SMPL-X、映射后预测 Mesh 和真值轮廓重叠。
4. **30 例失败闸门**：确认数字变化能被可视化失败解释后，才决定是否运行
   完整 450 例端到端对照。
5. **横向方法合同**：为 PIFu、PaMIR、ICON-no-filter、ICON-filter 固定同一
   manifest、坐标合同、采样数、分辨率、清理规则和失败率口径。

## 明天开工时的首要问题

不是立刻再跑 450 例，而是先回答：

> 官方入口与自建入口在同一 CAPE 先验条件下是否给出一致的汇总？

只有这把“尺子”校准后，PIXIE 端到端实验和不同论文横向比较才具有可信的
解释基础。

